## Что нового в v1.0.206

- Постраничная загрузка файлов — сначала показываются первые 200 файлов, остальные подгружаются по мере прокрутки
- При 1.5к+ файлах интерфейс больше не зависает — файлы появляются сразу

---

## What's new in v1.0.206

- Progressive file loading — first 200 files shown immediately, more loaded on scroll
- App no longer freezes with 1.5k+ files — files appear instantly
